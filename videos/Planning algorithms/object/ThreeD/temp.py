 # machine.rotate(
        #     angle=120 * DEGREES, axis=Z_AXIS, about_point=machine.get_center()
        # ).rotate(angle=-30 * DEGREES, axis=Y_AXIS, about_point=machine.get_center())
        # self.add(machine)